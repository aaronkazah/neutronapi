# Core dependencies (automatically installed with neutronapi)
uvicorn[standard]>=0.23.0
uvloop>=0.17.0  # Fast event loop for production
aiosqlite>=0.19.0
PyJWT>=2.8.0

# Optional dependencies
asyncpg>=0.29.0  # For PostgreSQL support